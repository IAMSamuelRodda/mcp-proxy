{
  "tools": {
    "browser_handle_dialog": {
      "description": "Handle browser dialog (alert, confirm, prompt). Set accept=true/false. For prompts, provide promptText.",
      "maps_to": "browser_handle_dialog",
      "server": "playwright",
      "inputSchema": {
        "type": "object",
        "properties": {
          "accept": {
            "type": "boolean",
            "description": "Whether to accept the dialog"
          },
          "promptText": {
            "type": "string",
            "description": "The text of the prompt in case of a prompt dialog"
          }
        },
        "required": ["accept"],
        "additionalProperties": false
      }
    }
  }
}
